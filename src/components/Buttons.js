import React  from 'react'

const Buttons = (props) => {

    return(
        <div> 

             <button onClick ={props.btnAction} > Fetch   </button>
             
        </div>
       
   
   
        )

   
}
export default Buttons;
